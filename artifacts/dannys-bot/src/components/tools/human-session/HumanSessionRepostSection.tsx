import type { ReactNode } from "react";
/** Isolated Make a Post/repost slot: history, caption and image-specific controls. */
export function HumanSessionRepostSection({children}:{children?:ReactNode}) { return <>{children}</>; }