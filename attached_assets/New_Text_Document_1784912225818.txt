SAME-ISSUE

THE-KEYBOARD-DOES-DISSAPEAR-VERY-QUICKYL-SO-ITS-EITHER-BEING-TAPPED-OUTSIDE-OF-IT-WHICH-WILL-CLOSE-IT-OR-SOMETHING-ELSE-ALL-I-KNOW-IS-YOU-CANT-BE-TAPPING-ANYWHERE-INSIDE-IT-BECAUSE-IT-WOULD-TYPE-ATLEAST-SOMETHING-SO-WHATEVER-THE-FUCK-YOUR-DOING-YOUR-WELL-OFF-AND-IM-GETTING-FUCKING-BORED-ID-ONT-UDNERSTAND-THE-DISCONNECT

29 minutes ago

3 actions
The error is explicit — adb shell input text calls KeyCharacterMap.getEvents() internally, which returns null for emoji (they're not in the hardware key map), causing the NPE. Text injection doesn't work for emoji on Android. I need to look at the actual Instagram UI that appears when the composer is tapped.


3 actions
When you tap the story compose bar, Instagram shows its own row of emoji reaction buttons (❤️ 🔥 etc.) in its UI layer — these ARE in the UIAutomator tree. Let me find out what they look like in the dumps.


4 actions
THOSE-ARE-PRE-LOADED-EMOTICOMS-WE-HAVE-MANY-ACCOUNTS-WE-CANT-USE-THE-SAME-ONES-OVER-AND-OVER-HENCE-WHY-WE-ARE-TRYING-TO-GET-THE-EMOJI-KEYBOARD-OPEN-TO-USE-THE-VAST-ARRAY-OF-THEM

27 minutes ago
Opening emoji keyboard

Opening emoji keyboard
Got it — the pre-loaded reactions are unacceptable because they repeat. The full emoji picker is needed.

adb shell input text throws a NullPointerException for emoji on this Android build — that's a hard platform limitation, not fixable.

The right answer: adb shell input keyevent 219 (KEYCODE_PICTSYMBOLS) — this switches the active keyboard into emoji/symbol mode instantly, no screencap needed. Then tap the grid as before. Let me check the keyevent helper and the grid tap logic.


3 actions
android.keyevent(serial, 219) — KEYCODE_PICTSYMBOLS — switches the active keyboard into emoji mode in one synchronous keyevent call. No screencap, no text injection. Writing the fix now.


7 actions
Pushed v1.2.138. The approach is now:

Tap composer → keyboard opens
adb shell input keyevent 219 (KEYCODE_PICTSYMBOLS) — switches the IME into emoji mode instantly, no image transfer, fires before anything can close the keyboard
Random scroll through the picker (1–50 swipes)
Tap a random grid position → full emoji variety
Tap send
Three different root causes across three versions — all documented in the changelog. If 219 doesn't switch the keyboard on this specific MIUI build, send a log and I'll look at what keycode the MIUI keyboard uses for its emoji switch.


Worked for 4 minutes

Attached Image

Attached Image
NOPE

EMOJI-PICKER-IS-NEVER-PICKED

JUST-TYPING-BECAUSE-OF-THE-FAILING-SCROLLING-AS-NO-EMOJI-PICKER-APPEARED-SO-WHERE-DO-WE-GO-FROM-HERE-THIS-IS-YOUR-50TH-ATTEMPT-TO-FIX-THIS-WE-NEED-TO-STOP-BACK-AND-RE-CALCULATE-WHY-THE-FUCK-YOU-CANT-PRESS-TAP-ON-A-FUCKING-KEYBOARD-PAD-WITH-A-SMILEY-FACE-EMOTICOM-A-COORDINATES-FIX-WOULD-WORK-BUT-WE-USE-DIFFERENT-DEVICES-SO-THEN-IT-WOULD-FAIL-AND-DIFFERENT-DEVICES-WOULD-SHOW-A-DIFFERENT-SIZED-KEYBOARD-ITS-PATHETIC-THIS

10 minutes ago
Observing keyboard elements

Observing keyboard elements
The screenshots tell me everything I need. The keyboard is open and the emoji key is right there in the bottom row — KEYCODE_PICTSYMBOLS just did nothing on this keyboard (didn't switch mode), and then the swipe scroll ran on the QWERTY keyboard instead.

Let me look at what dumpUi captures and what Gboard exposes in the a11y tree, plus check the screencap decode path with actual diagnostics.


Show less

Searched for "dumpUi|uiautomator|dump…"

Searched for "emoji.*key|smiley|switc…"
Analyzing UI dump and IME issues

Analyzing UI dump and IME issues

Opened androidManager.ts

Opened androidManager.ts
Analyzing UIAutomator dump

Analyzing UIAutomator dump
The existing typing code already knows how to find keyboard keys from UIAutomator. Let me read that function — it uses a keyMap — plus the switchToSymbols function which is exactly what we need for the emoji tab.


Show less

Searched for "switchToSymbols|switchT…"

Opened androidManager.ts
Extracting keyboard key data

Extracting keyboard key data
I can see exactly what happened in the screenshots. The keyboard is open and QWERTY is visible — KEYCODE_PICTSYMBOLS did nothing on this keyboard, so the scroll swipes ran on the QWERTY layout instead of an emoji picker.

The definitive fix: find the space bar in the UIAutomator dump (it IS in the tree — standard widget, always exposed), then tap immediately to its left. The key height from the bounds gives the key width estimate. Device-agnostic, no screencap, no keycodes. Let me read the section I'm editing then write the fix.


Show less

Opened mobile.ts
Implementing space-bar UIAutomator

Implementing space-bar UIAutomator

Edited mobile.ts
Completing task

Completing task

Processed text

Edited CHANGELOG.md