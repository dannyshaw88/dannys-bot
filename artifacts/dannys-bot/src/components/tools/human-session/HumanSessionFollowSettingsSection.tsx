import type { ReactNode } from "react";
/** Shared slot for the follow settings block (including injection and SAV controls). */
export function HumanSessionFollowSettingsSection({children}:{children?:ReactNode}) { return <>{children}</>; }