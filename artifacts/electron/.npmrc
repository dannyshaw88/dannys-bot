# Prevent puppeteer from downloading Chrome during npm install on the build
# machine. The app downloads its own Chrome for Testing at first EB launch
# via @puppeteer/browsers, storing it in the user's app-data folder.
puppeteer_skip_download=true
