name: (Deprecated) Windows Installer

# DEPRECATED — 6 Jul 2026: this workflow was a near-duplicate of
# `build-windows-installer.yml`. Consolidated into `build-windows-installer.yml`,
# which is now the single canonical workflow for building the Windows
# installer. Deletion of this tracked file is blocked in the current
# environment, so it is kept as an inert stub with no triggers instead.
#
# Do not add a `push` or `workflow_dispatch` trigger back to this file — use
# `build-windows-installer.yml` for any future changes to the build/release
# pipeline.

on:
  workflow_call:
    inputs:
      note:
        required: false
        type: string
        default: "deprecated — see build-windows-installer.yml"

jobs:
  deprecated-noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. See build-windows-installer.yml."
